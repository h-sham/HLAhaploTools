% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_MAC_to_alpha.R,
%   R/reformat_typing_data.R
\name{reformat_typing_data}
\alias{reformat_typing_data}
\title{Reformat Raw HLA Typing Data}
\usage{
reformat_typing_data(df)

reformat_typing_data(df)
}
\arguments{
\item{df}{A data frame or tibble containing raw HLA typing data.}
}
\value{
A cleaned and standardized tibble with sorted columns, proper allele format,
and rows ordered by family.

A cleaned and standardized tibble with sorted columns, proper allele format,
filled missing second alleles, and rows ordered by family.
}
\description{
Cleans and reformats raw HLA typing data for downstream analysis.
Includes trimming whitespace, column sorting, and ensuring allele fields
are in full gene format (e.g., A\emph{01:01).
If it starts with * (e.g., \emph{01:02), prepend the gene name only (e.g., A}01:02).
Else if it does not contain * at all (e.g., 01:02), prepend gene + * (e.g., A}01:02).
Else (already has gene prefix, e.g., A*01:02), leave as is.
Rows are reordered by FAMILY_ID and Family_Member with levels F, M, C1, C2.

Cleans and reformats raw HLA typing data for downstream analysis.
Includes trimming whitespace, column sorting, and ensuring allele fields
are in full gene format (e.g., A\emph{01:01).
If it starts with * (e.g., \emph{01:02), prepend the gene name only (e.g., A}01:02).
Else if it does not contain * at all (e.g., 01:02), prepend gene + * (e.g., A}01:02).
Else (already has gene prefix, e.g., A*01:02), leave as is.
Missing second alleles (_2) will be filled with first allele (_1) if present.
Rows are reordered by FAMILY_ID and Family_Member with levels F, M, C1, C2.
}
