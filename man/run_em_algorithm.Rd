% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EM.R
\name{run_em_algorithm}
\alias{run_em_algorithm}
\title{Run EM Algorithm for Haplotype Frequency Estimation}
\usage{
run_em_algorithm(
  all_diplotypes,
  epsilon = 1e-05,
  max_iter = 100,
  parallel = TRUE,
  n_workers = NULL,
  quiet = FALSE
)
}
\arguments{
\item{all_diplotypes}{A list of diplotype lists, one per subject.}

\item{epsilon}{Convergence threshold (default = 1e-5).}

\item{max_iter}{Maximum number of iterations (default = 100).}

\item{parallel}{Logical. Controls parallel execution. If TRUE (default), uses parallel processing
when available and falls back to sequential if not. If FALSE, forces sequential.}

\item{n_workers}{Number of workers for parallel processing. If NULL, use available cores - 1.}

\item{quiet}{Logical. If TRUE, suppresses status messages.}
}
\value{
A named vector of estimated haplotype frequencies.
}
\description{
Implements the expectation-maximization algorithm to estimate haplotype frequencies
from genotype data. Automatically uses parallel processing if available.
}
\note{
The implementation of this function was developed with assistance from
Claude Sonnet 3.7 Thinking.
}
\examples{
\dontrun{
# Run EM algorithm with automatic parallelization
freq_est <- run_em_algorithm(all_diplotypes)

# Force sequential processing
freq_est <- run_em_algorithm(all_diplotypes, parallel = FALSE)
}
}
