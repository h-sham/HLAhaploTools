% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EM.R
\name{assign_HLA_posteriors}
\alias{assign_HLA_posteriors}
\title{Assign Posterior Diplotypes (Full &/or Top) Per Individual}
\usage{
assign_HLA_posteriors(diplotype_list, hap_freqs, ids = NULL, output = "both")
}
\arguments{
\item{diplotype_list}{A list (length = n subjects) of diplotype candidates.
Each element is a list of lists with fields \code{hap1} and \code{hap2}.}

\item{hap_freqs}{A named numeric vector of inferred haplotype frequencies.}

\item{ids}{Optional vector of subject IDs (length = n). Defaults to 1:n.}

\item{output}{Character. Which result to return:
\itemize{
\item \code{"both"}: return both full posterior list and top diplotypes (default)
\item \code{"full"}: return only the full posterior list
\item \code{"top"}: return only the top diplotypes tibble
}}
}
\value{
Depending on \code{output}:
\itemize{
\item A \strong{list} with \code{posteriors} (list of tibbles) and \code{top_diplotypes} (tibble),
\item Or solely the \code{posteriors} list,
\item Or solely the \code{top_diplotypes} tibble.
}
}
\description{
Computes posterior probabilities for all diplotypes per subject, deduplicates
reverse-equivalent pairs, and optionally returns both the full list and the
single top call.
}
